import React from 'react';

import styles from './styles.css';

export default () => (
    <div class={styles.wrapper}>
        <div class={styles.loader}>
            <div />
        </div>
    </div>
);
